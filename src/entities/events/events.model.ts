export type EventsElement = {
    lastDate?: string
    event?:string
    cnt?:string
    unsaved?: boolean
    unregistered?: boolean
}

export type FilterEventsElement = {
    fromDate?: string
    toDate?: string
}
